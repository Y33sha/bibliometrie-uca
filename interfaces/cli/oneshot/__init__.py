"""Scripts CLI à usage unique (backfilling, migrations de données).

Distinct de `interfaces/cli/maintenance/` qui regroupe les outils
ad-hoc réutilisables (fusions manuelles, merges). Ici : scripts
jetables après exécution, généralement attachés à un chantier DATA.
"""
