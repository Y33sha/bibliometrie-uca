"""Concept métier Journal — aggregate root et règles associées.

Sous-modules :
- `journal` : aggregate root `Journal` (table `journals`).

Scaffolding a minima : pas d'invariants métier rapatriés ici, à
enrichir lors d'un chantier ultérieur si besoin.
"""
