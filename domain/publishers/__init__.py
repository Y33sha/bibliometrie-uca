"""Concept métier Publisher — aggregate root et règles associées.

Sous-modules :
- ``publisher`` : aggregate root ``Publisher`` (table `publishers`).

Scaffolding a minima : pas d'invariants métier rapatriés ici, à
enrichir lors d'un chantier ultérieur si besoin.
"""
