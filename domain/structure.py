"""Concept métier Structure.

Aujourd'hui ce module est presque vide : il accueillera les value
objects (ROR, RNSR) et entités métier `Structure` au fur et à mesure
qu'elles gagnent des invariants. Le modèle de la colonne JSONB
`structures.api_ids` vit côté infrastructure
(`infrastructure/db/jsonb_models/structure.py`) — c'est un détail
d'adapter de persistance, pas du métier.
"""
