# bibliometrie-uca — dépendances Python (versions figées)
# Toutes les versions sont pinned (==) pour reproductibilité.
# Upgrader via : pip install <pkg>==<version> && mettre à jour ici.

# ---- Runtime ----
requests==2.33.0          # CVE-2024-35195, CVE-2024-47081, CVE-2026-25645 fixed
psycopg2-binary==2.9.11
fastapi==0.128.2
uvicorn==0.40.0
bcrypt==5.0.0
python-dotenv==1.2.1
pydantic-settings==2.13.1
openpyxl==3.1.5           # utilisé par scripts/import_persons.py

# ---- Dev / tests ----
pytest==9.0.3             # CVE-2025-71176 fixed
pytest-cov==7.1.0
httpx==0.27.2
ruff==0.15.11
mypy==1.20.1
